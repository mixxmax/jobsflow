claude:
  interception: 'runtime: PreToolUse hook (JSON decision)'
  terminal: git pre-push hook + CI gate
  live_verified: false
  note: Protocol covered by tests; live API key not required for product clones
opencode:
  interception: 'runtime: .opencode/plugins tool.execute.before (raise to block)'
  terminal: git pre-push hook + CI gate
  live_verified: true
  live_evidence: Sandbox drill — Edit under .sopcontrol rejected by plugin
codex:
  interception: none (no pre-tool hook in 0.147)
  terminal: git pre-push hook + CI gate + sopctl wrap (post-hoc)
  advisory: AGENTS.md rule projection (sopctl project codex)
  live_verified: true
  live_evidence: Sandbox drill — wrap post-hoc gate blocked on fail rules
